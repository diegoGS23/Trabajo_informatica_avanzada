#include "animal.h"
#include <QVector>
#include <QDebug>

Animal::Animal(int id, QString especie, QString nombre, int edad):id(id),especie(especie),nombre(nombre),edad(edad) {

}

void Animal::actualizarEstado(){

}
